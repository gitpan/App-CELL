# CELL_DEBUG_MODE
#     0  no debugging messages are generated
#     1  lots of debugging messages are sent to syslog
set( 'CELL_DEBUG_MODE', 0 );

# CELL_DISTRO_SHAREDIR_FULLPATH
#        full path of App::CELL distro sharedir
#        overrided by site param when sharedir is loaded
set('CELL_DISTRO_SHAREDIR_FULLPATH', '');

# CELL_SITECONF_DIR_FULLPATH
#        full path of siteconf dir
#        overrided by site param when siteconf dir is loaded
set('CELL_SITECONF_DIR_FULLPATH', '');

